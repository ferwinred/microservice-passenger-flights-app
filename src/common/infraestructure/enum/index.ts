export enum Dialect {
    POSTGRES = 'postgres',
    MYSQL = 'mysql',
    MARIADB = 'mariadb',
    ORACLE = 'oracle',
} 

export enum PassengerState {
    INACTIVE = 'inactive',
    ACTIVE = 'active',
}